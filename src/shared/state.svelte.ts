/* State HERE! */
export const panelState = $state({
  visible: false,
  tab: "World",
});

export const editState = $state({
  name: "",
});

export const travelState = $state({
  text: "",
  graphic: "",
});
